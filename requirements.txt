# Web UI
streamlit>=1.58.0

# OpenAI (speaker notes, slide plans, presentations)
openai>=1.82.0

# Environment variables (.env)
python-dotenv>=1.1.0

# PowerPoint read/write (python-pptx)
python-pptx>=1.0.2

# ElevenLabs text-to-speech API
requests>=2.32.0

# Embed audio into .pptx via PowerPoint COM (Windows only)
comtypes>=1.4.0; sys_platform == "win32"
pywin32>=306; sys_platform == "win32"
